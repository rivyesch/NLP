Run using myCore.py

Excel files can be created by uncommenting lines 66-68

Figures created using data obtained by manually running. Data stored in dataNotes.txt

Dataset Original is the data as IS sent - This has not been included here as it is an extremely large file and would be over the submission limit.
Dataset Subset 1 is 10 randomly selected case files. Takes ~10 minutes to run
Dataset Subset 2 is 4 randomly selected case files. Can be executed in a reasonable time, while still getting good results.
Dataset Subset 3 is 1 randomly selected case file. Used for testing the code itself.